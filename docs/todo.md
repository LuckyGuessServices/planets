
# TODO

## MVP

1. - [x] Create an API server controller `/api` with a "health-check" endpoint.
    1. - [x] Add `/` (or "index") GET endpoint. No request parameters. Returns:
        * name of the service;
        * current date and time with a time zone generated by the service.
    1. - [x] Test it manually.
    1. - [x] Cover with an autotest.
        * - [x] Try not to use an actual http server, call an API method as an internal method instead
            (no extra traffic).
1. - [ ] Add a long-term storage database.
    1. - [ ] Set up a connection. Store secrets in any kind of config file.
    1. - [ ] Update `/` endpoint: add the current date and time with a time zone generated by the database.
    1. - [ ] Update the autotest.
1. - [ ] Add `mercuryretrogradeapi.com` API client. See [docs](https://mercuryretrogradeapi.com/about.html).
    1. - [ ] Implement a method for the only endpoint `/` and its optional parameter `date`
         (but consider it as a mandatory parameter).
    1. - [ ] Cover it with _manually launched_ autotests - should not be launched for each commit.
1. - [ ] Add `on-date` GET endpoint. Utilize the API client created on the previous step.
    1. - [ ] Definition:
        * Request parameters:
            * date: `YYYYMMDD` or any automatically recognizable format
        * Response:
            * array of objects, each: (int) planet_id => (bool) is_retrograde
    1. - [ ] If there is no data locally, request the external API, store the data locally and then return the data.
        * Add a `TODO` comment to store data in background.
    1. - [ ] Consider edge cases:
        * Invalid date.
        * No data in the local storage, external APIs are unavailable (no response or time out).
    1. - [ ] Cover with autotests. Mock external API(s).
        * Cases: data from external API, data from the local storage, invalid date, no data available.
        * Try to explicitly forbid external connections - if you add a new external API and forget to update autotests,
          the latter will fail while trying to request an external IP.
1. - [ ] Set up a dedicated http server if necessary.
1. - [ ] Replace a server port constant with a config value.
     Pass the real value via a docker compose file or by some other ways.
1. - [ ] Replace `log.*` calls with logging to files.

## Misc

1. - [ ] Make a way in tests to specify just then ending part of an endpoint URI (like `/` instead of `/api/`),
     (?) but try using a global router at the same time.
1. - [ ] Cover [api.go](../src/api/api.go) functions with tests.
    1. - [ ] Stripping URI prefixes from API endpoints' URIs.
    1. - [ ] Edge cases with errors.
